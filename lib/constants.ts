// Общие константы (клиент + сервер).

// Максимум фото-референсов в ОДНОМ запросе к Gemini (не считая IMAGE 1 = дом).
// Много референсов → модель «галлюцинирует». Всё сверх лимита уходит текстовой
// подсказкой (hint), а не картинкой. Используется в:
//   • app/api/visualize/route.ts — реальное ограничение бюджета
//   • components/Visualizer.tsx   — индикатор «N / MAX» в каталоге материалов
export const MAX_REFERENCE_IMAGES = 4;
